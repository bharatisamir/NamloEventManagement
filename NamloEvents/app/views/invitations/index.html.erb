<!--


<h1>Invitations</h1>

<table>
  <thead>
    <tr>
      <th>Opening tag line</th>
      <th>Groom tag line</th>
      <th>Mid tag line</th>
      <th>Bride tag line</th>
      <th>Event tag line</th>
      <th>Gift tag line</th>
      <th>Closing tag line</th>
      <th>Event</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @invitations.each do |invitation| %>
      <tr>
        <td><%= invitation.opening_tag_line %></td>
        <td><%= invitation.groom_tag_line %></td>
        <td><%= invitation.mid_tag_line %></td>
        <td><%= invitation.bride_tag_line %></td>
        <td><%= invitation.event_tag_line %></td>
        <td><%= invitation.gift_tag_line %></td>
        <td><%= invitation.closing_tag_line %></td>
        <td><%= invitation.event %></td>
        <td><%#= link_to 'Show', invitation %></td>
        <td><%#= link_to 'Edit', edit_invitation_path(invitation) %></td>
        <td><%#= link_to 'Destroy', invitation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'New invitation', new_invitation_path %>
-->
